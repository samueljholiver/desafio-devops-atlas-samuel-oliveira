apiVersion: v1
kind: Secret
metadata:
  name: orders-api-secrets
  labels:
    app.kubernetes.io/name: orders-api
# Valores placeholder — em produção, usar External Secrets Operator ou
# Azure Key Vault Provider for Secrets Store CSI Driver para injetar
# segredos direto do Azure Key Vault.
type: Opaque
stringData:
  ConnectionStrings__DefaultConnection: "PLACEHOLDER_INJECTED_BY_KEYVAULT"
  BlobStorage__ConnectionString: "PLACEHOLDER_INJECTED_BY_KEYVAULT"
